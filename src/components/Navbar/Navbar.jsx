import React from 'react';
import { Input } from '../generic';

export function Navbar() {
	return (
		<div className="container flex flex-wrap items-center justify-center mx-auto p-5">
			<Input/>
		</div>
	);
}
